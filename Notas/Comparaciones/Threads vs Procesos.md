Podemos comparar procesos con thread observando que un thread está contenido en un proceso, pero el proceso agrupa recursos y el thread es una entidad programada (scheduled) para ejecución en el CPU. 

Por otro lado, los threads permiten múltiples ejecuciones en el mismo conjunto de recursos. Tener múltiples threads en un proceso es análogo a tener múltiples procesos en una PC.